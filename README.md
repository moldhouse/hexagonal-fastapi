# Python Default

Ideas on how to configure and setup a Python project.

## Getting Started

```sh
uv sync
```


Run the scheduler with

```sh
uv run python -m src.main
```

Run the tests with

```sh
uv run pytest
```